apiVersion: v1
kind: Service
metadata:
  name: travel-together-grafana
  labels:
    app: travel-together-grafana
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: travel-together-grafana
