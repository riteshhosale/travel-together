
{
  "buildCommand": "npm --prefix frontend/client run build",
  "outputDirectory": "frontend/client/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
